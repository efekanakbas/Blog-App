/**
 * Routes for everbody 
 * @type {string[]}
 */
export const publicRoutes = [
   
]

/**
 * Auth routhes for authentications
 * @type {string[]}
 */
export const authRoutes = [
    "/login", "/register", "/confirm"
]


/**
 * Redirect URL after login
 * @type {string}
 */
export const DEFAULT_LOGIN_REDIRECT = "/"